<%= form_for(@player) do |f| %>
  <% if @player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</h2>

      <ul>
      <% @player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :jersey_number %><br />
    <%= f.number_field :jersey_number, :in => 0...99  %>
  </div>
  <div class="field">
    <%= f.collection_select( :team_id, @teams, :id, :company_name)  %>
  </div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
